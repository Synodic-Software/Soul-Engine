#pragma once

#include <string>
#include "Utility\Includes\GLFWIncludes.h"

/* A buffer. */
class Buffer {
public:

	/* Default constructor. */
	Buffer();
	/* Destructor. */
	virtual ~Buffer();

protected:


private:
	

};