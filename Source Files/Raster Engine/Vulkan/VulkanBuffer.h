#pragma once

#include <string>
#include "Raster Engine/Buffer.h"

/* Buffer for vulkan. */
class VulkanBuffer : public Buffer{
public:

	/* Default constructor. */
	VulkanBuffer();
	/* Destructor. */
	virtual ~VulkanBuffer();

protected:


private:
	

};