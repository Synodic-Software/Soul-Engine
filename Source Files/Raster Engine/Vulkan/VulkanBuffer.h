#pragma once

#include <string>
#include "Raster Engine/Buffer.h"

/* Buffer for vulkan. */
/* Buffer for vulkan. */
class VulkanBuffer : public Buffer{
public:

	/* Default constructor. */
	/* Default constructor. */
	VulkanBuffer();
	/* Destructor. */
	/* Destructor. */
	virtual ~VulkanBuffer();

protected:


private:
	

};