#pragma once

#include <string>
#include "Utility\Includes\GLFWIncludes.h"

/* A buffer. */
/* A buffer. */
class Buffer {
public:

	/* Default constructor. */
	/* Default constructor. */
	Buffer();
	/* Destructor. */
	/* Destructor. */
	virtual ~Buffer();

protected:


private:
	

};